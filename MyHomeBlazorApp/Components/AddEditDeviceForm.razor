@using MyHomeBlazorApp.BlazorData
@using MyHome.Models;
@using MyHome;
@using MyHomeBlazorApp.Components.Modals
@using MyHomeBlazorApp.Pages
@using MyHomeBlazorApp.Shared
@using System.Reflection
@using MyHomeBlazorApp.Components
@inject BlazorData.DataService DataService
@inject IJSRuntime JSRuntime
@inject NavigationManager uriHelper
@inject NavigationManager NavManager

<style>
    .btn-row {
        display: flex;
        justify-content: space-between;
        align-items: center;
    }

    .form-select {
        max-width: auto;
        padding-left: 10px;
        padding-right: 0;
    }

    .left-buttons button,
    .right-buttons button {
        margin: 0 5px;
    }

    .form-group {
        margin-bottom: 10px;
    }
</style>

@if (showErrorMessageVisible)
{
    <div class="alert alert-danger">
        @errorMessage
    </div>
}
@if (CurrentDevice != null)
{
    <EditForm @ref="refAddEditDeviceTest" EditContext="@editContext" id="refAddEditDeviceTest" OnSubmit="Save" @oninput="OnInput">
        <DataAnnotationsValidator />
        <div class="form-group row">
            <label for="deviceName" class="col-sm-4 col-form-label">
                Name
            </label>
            <div class="col-sm-8">
                <InputText id="deviceName" @bind-Value="CurrentDevice.DeviceName" placeholder="Device Name" class="form-control" />
            </div>
            <ValidationMessage For=@(() => CurrentDevice.DeviceName) />
        </div>
        <div class="form-group row">
            <label for="deviceProduser" class="col-sm-4 col-form-label">
                Produser
            </label>
            <div class="col-sm-8">
                <InputText id="deviceProduser" @bind-Value="CurrentDevice.DeviceProduser" placeholder="Device Produser" class="form-control" />
            </div>
        </div>
        <div class="form-group row">
            <label for="deviceType" class="col-sm-4 col-form-label">Type</label>
            <div class="col-sm-8">
                <InputSelect id="deviceType" @bind-Value="CurrentDevice.DeviceType" placeholder="Device Type" class="form-select">
                    @foreach (var type in Enum.GetValues(typeof(DeviceType)))
                    {
                        <option value="@type">@type</option>
                    }
                </InputSelect>
            </div>
        </div>
        <div class="form-group row">
            <label for="deviceSerialNumber" class="col-sm-4 col-form-label">
                Serial Number
            </label>
            <div class="col-sm-8">
                <InputText id="deviceSerialNumber" @bind-Value="CurrentDevice.DeviceSerialNumber" placeholder="Device Serial Number" class="form-control" />
            </div>
        </div>
        <div class="form-group row">
            <label for="deviceModelNumber" class="col-sm-4 col-form-label">
                Model Number
            </label>
            <div class="col-sm-8">
                <InputText id="deviceModelNumber" @bind-Value="CurrentDevice.DeviceModelNumber" placeholder="Device Model Number" class="form-control" />
            </div>
        </div>
        <AuthorizeView Context="auth">
            <Authorized>
                <div class="form-group row">
                    <label for="deviceIpAddress" class="col-sm-4 col-form-label">
                        IP Address
                    </label>
                    <div class="col-sm-8">
                        <InputText id="deviceIpAddress" @bind-Value="CurrentDevice.IpAddress" placeholder="Device Ip Address" class="form-control" />
                    </div>
                </div>
                <div class="form-group row">
                    <label for="deviceMacAddress" class="col-sm-4 col-form-label">
                        Mac Address
                    </label>
                    <div class="col-sm-8">
                        <InputText id="deviceMacAddress" @bind-Value="CurrentDevice.MacAdrress" placeholder="Mac Address" class="form-control" />
                    </div>
                </div>
            </Authorized>
            <NotAuthorized>

            </NotAuthorized>
        </AuthorizeView>
        <div class="form-group row">
            <label for="deviceManualBookLink" class="col-sm-4 col-form-label">
                Manual Link
            </label>
            <div class="col-sm-8 align-content-center">
                <Button class="btn-sm btn btn-outline-info" @onclick="() => DataService.Navigate(CurrentDevice, JSRuntime)">Manual</Button>
            </div>
        </div>
        <div class="row mt-3">
            <div class="form-group">
                @if (CurrentUser != null)
                {

                    <label class="form-label">Select Real Estate:</label>
                    <InputSelect @bind-Value="RealEstateIdToAddDevice" class="form-select" @bind-Value:after="RealEstateIdToAddDeviceChanged">
                        @if (CurrentUser.RealEstates == null || !CurrentUser.RealEstates.Any())
                        {
                            @* Option: User has NO real estate yet *@
                            <option value="0">No Real Estates found (Save as Unassigned)</option>
                        }
                        else
                        {
                            <option selected value="0">Select Real Estate...or Will Be Saved As Unnassigned</option>
                            @foreach (var realEstate in CurrentUser.RealEstates)
                            {
                                <option value="@realEstate.RealEstateID">@realEstate.RealEstateName</option>
                            }
                        }
                    </InputSelect>

                    @if (CurrentUser?.RealEstates == null || !CurrentUser.RealEstates.Any())
                    {
                        <small class="text-muted d-block mt-1">
                            Tip: You can move this to a Real Estate later.
                        </small>
                    }
                }
                @* <ValidationMessage For="() => selectedTargetRealEstateId.RealEstateID" class="text-danger d-block" /> *@

            </div>
        </div>

        @if (showErrorMessageVisible == true)
        {
            <p class="text-danger d-block">@errorMessage</p>
        }
        <br />
        @if (_isGuest)
        {
            <div class="alert alert-light py-1">
                <small>Log in to make changes or see more details.</small>
            </div>
            <div class="text-center">
                <Button Class="btn-outline-danger"
                        @onclick="NavigateToLoginWithReturnUrl">
                    Login
                </Button>
            </div>
        }
        else
        {
            <br />
            <div class="btn-row">
                <div class="left-buttons col-sm-6 ">
                    <NextPreviuosButtons CurrentDevice ="@CurrentDevice"
                                         DevicesListAsQueryable="_devicesListAsQueryable"
                                         CurrentDeviceChanged="HandleDeviceSwitch">
                    </NextPreviuosButtons>
                </div>
                <div class="right-buttons col-sm-6 text-end">
                    <ButtonSave ButtonEvent="Save" Form="refAddEditDeviceTest" IsButtonDisabled="@_saveDisabled" />
                    @if (IncludeButtons)
                    {
                        <ButtonCloseCancel TextButton="Cancel" ButtonEvent="@(() => Parent?.HideAsync())" />
                    }
                </div>
            </div>
        }
    </EditForm>

}
<div>
    <SuccessModal @ref="_refSuccessModal" Title="Successful action!"></SuccessModal>
</div>


@code {

    [Parameter]
    public DeviceProfile CurrentDevice { get; set; } = new();
    [Parameter]
    public UserProfile CurrentUser { get; set; } = new();
    [Parameter]
    public EditForm refAddEditDeviceTest { get; set; } = new();
    [CascadingParameter]
    public CustomReusableModal? Parent { get; set; }
    [Parameter]
    public EventCallback DataChangedEvent { get; set; }

    public int RealEstateIdToAddDevice
    {
        get => selectedTargetRealEstateId;
        set
        {
            selectedTargetRealEstateId = value;
        }
    }
    private int selectedTargetRealEstateId;
    private IQueryable<DeviceProfile>? _devicesListAsQueryable { get; set; }
    private IQueryable<DeviceProfile>? _unassignedDevicesListAsQueryable { get; set; }
    private EditContext? editContext { get; set; }

    private SuccessModal _refSuccessModal = default!;
    private string? errorMessage;
    private bool choseRealEstateIsValid;
    private bool IncludeButtons { get; set; } = false;
    private bool deviceIsAssignedToRealEstate;
    private bool validDeviceForm;
    private bool showErrorMessageVisible = false;
    private bool fieldHasBeenChanged { get; set; } = false;
    private bool _isGuest => DataService.CurrentAppUser == null;    
    private bool _saveDisabled => !fieldHasBeenChanged;

    private void HandleDeviceSwitch(DeviceProfile targetDevice)
    {
        
        fieldHasBeenChanged = false;
        CurrentDevice = targetDevice;        
        editContext = new(CurrentDevice);
    }

    private void NavigateToLoginWithReturnUrl()
    {
        var currentUri = NavManager.ToBaseRelativePath(NavManager.Uri);
        var escapedReturnUrl = Uri.EscapeDataString(currentUri);
        NavManager.NavigateTo($"account/login?returnUrl={escapedReturnUrl}", forceLoad: true);
    }

    private void OnInput()
    {
        fieldHasBeenChanged = true;
    }

    protected override async Task OnInitializedAsync()
    {
        // Just try to hydrate the DataService if a session exists
        await DataService.LoadUserWithAllDataAsync();
        Init();
    }
    //todo: disable button if there is no changes in the editForm
    private async Task Init()
    {
        if (CurrentDevice == null)
        {
            return;
        }

        // 1. Initialize the form context (this works for guests!)
        editContext = new(CurrentDevice);

        // 2. Only fetch the "full list" if someone is actually logged in
        // This prevents the "No User Logged In" error for guests
        if (DataService.CurrentAppUser != null)
        {
            var allDevicesList = DataService.GetAllUserDevices();
            _devicesListAsQueryable = allDevicesList.AsQueryable();
        }
        else
        {
            // For guests, we provide an empty list or just the current device
            // so the UI components don't break
            _devicesListAsQueryable = new List<DeviceProfile> { CurrentDevice }.AsQueryable();
        }
    }

    public async Task Save()
    {
        if (editContext != null)
        {
            await DeviceFormSubmitted(editContext);
        }
    }

    protected override void OnParametersSet()
    {
        if (CurrentDevice == null)
        {
            return;
        }

        if (Parent != null)
        {
            IncludeButtons = true;
        }

        if (CurrentDevice.DeviceID == 0)
        {
            selectedTargetRealEstateId = 0;
        }
        else
        {
            selectedTargetRealEstateId = DataService.GetRealEstateByDeviceID(CurrentDevice.DeviceID);
        }
        // var allDevicesList = DataService.GetAllUserDevices();
        // _devicesListAsQueryable = allDevicesList.AsQueryable();

        editContext = new(CurrentDevice);
        fieldHasBeenChanged = false;

        base.OnParametersSet();
    }

    private void RealEstateIdToAddDeviceChanged()
    {
        fieldHasBeenChanged = true;
        showErrorMessageVisible = (selectedTargetRealEstateId == 0);
    }


    private async Task DeviceFormSubmitted(EditContext editContext)
    {
        // 1. Validate data annotations
        if (!editContext.Validate()) return;

        bool isNewDevice = CurrentDevice.DeviceID == 0;
        bool isCurrentlyAssigned = false;

        if (!isNewDevice)
        {
            // Check if the device is currently linked to a RealEstate
            isCurrentlyAssigned = DataService.GetRealEstateByDeviceID(CurrentDevice.DeviceID) > 0;
        }

        try
        {
            // 2. Decision Logic
            switch (isNewDevice, selectedTargetRealEstateId)
            {
                // Case 1: Add new device
                case (true, _):
                    await DataService.AddNewDeviceToDataBase(CurrentDevice, selectedTargetRealEstateId);
                    break;

                // Case 2: Existing device moved to "Unassigned" (Target ID 0)
                case (false, 0) when isCurrentlyAssigned:
                    await DataService.MoveDeviceToUnnassignedList(CurrentDevice);
                    break;

                // Case 3: Move from Unassigned list to a specific Real Estate
                case (false, var targetId) when !isCurrentlyAssigned && targetId != 0:
                    await DataService.MoveDeviceFromUnassignedDevicesProfile(CurrentUser, targetId, CurrentDevice);
                    break;

                // Case 4: Transfer from one Real Estate to another
                case (false, var targetId) when targetId != 0:
                    await DataService.MoveDeviceToOtherRealEstate(CurrentDevice.DeviceID, targetId);
                    break;

                default:
                    // Just update fields (Name, Serial Number, etc.) without changing location
                    await DataService.UpdateObjectInDB();
                    break;
            }

            // Finalize
            await DataService.UpdateObjectInDB();
            await DataChangedEvent.InvokeAsync();

            // Reset state
            selectedTargetRealEstateId = 0;
            showErrorMessageVisible = false;
            fieldHasBeenChanged = false;

            if (Parent != null)
            {
                await Parent.HideAsync();
            }
            else
            {
                await _refSuccessModal.OnShowModalClick();
            }
        }
        catch (Exception ex)
        {
            errorMessage = "Database error: " + ex.Message;
            showErrorMessageVisible = true;
        }
    }
}
