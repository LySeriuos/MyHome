@using BlazorBootstrap
@inject BlazorData.DataService DataService
@using MyHome.Models


<EditForm Model="@CurrentObject">
    <div class="form-group row">
        <label for="streetName" class="col-sm-2 col-form-label">
            Street Name
        </label>
        <div class="col-sm-10">
            <InputText id="streetName" class="form-control" placeholder="Street Name"
                       @bind-Value="@CurrentObject.Address.StreetName" />
        </div>
    </div>
    <div class="form-group row">
        <label for="houseNumber" class="col-sm-2 col-form-label">
            House Number
        </label>
        <div class="col-sm-10">
            <InputNumber id="houseNumber" class="form-control" placeholder="House Number"
                         @bind-Value="@CurrentObject.Address.HouseNumber" />
        </div>
    </div>
    <div class="form-group row">
        <label for="houseNumberExtension" class="col-sm-2 col-form-label">
            House Number Extension
        </label>
        <div class="col-sm-10">
            <InputText id="houseNumberExtension" class="form-control" placeholder="House Number Extension"
                       @bind-Value="@CurrentObject.Address.HouseNumberExtension" />
        </div>
    </div>

    <div class="form-group row">
        <label for="city" class="col-sm-2 col-form-label">
            City
        </label>
        <div class="col-sm-10">
            <InputText id="city" class="form-control" placeholder="City"
                       @bind-Value="@CurrentObject.Address.City" />
        </div>
    </div>
    <div class="form-group row">
        <label for="country" class="col-sm-2 col-form-label">
            Country
        </label>
        <div class="col-sm-10">
            <InputText id="country" class="form-control" placeholder="Country"
                       @bind-Value="@CurrentObject.Address.Country" />
        </div>
    </div>
</EditForm>


@code {

    Address? Address = new Address();

    [Parameter]
    public RealEstate? CurrentObject { get; set; }


}
